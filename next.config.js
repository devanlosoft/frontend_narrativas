/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.es.wired.com', 'img.freepik.com', 'i.blogs.es', 'images.unsplash.com'],
      },
};

module.exports = nextConfig;
